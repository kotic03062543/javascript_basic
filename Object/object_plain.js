// plain object (ออบเจ็คพื้นฐาน)
// เป็น object ทั่วไปที่สร้างด้วย {} และกำหนด key: value
const plainObject = {
  name: "John",
  age: 30,
  isProgrammer: true,
};
console.log(plainObject);
console.log(plainObject.name);
console.log(plainObject["age"]);
